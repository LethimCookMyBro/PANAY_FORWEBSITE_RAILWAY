{
  "name": "panya",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && cd ../frontend && npm run build",
    "start": "npx serve frontend/dist -s -l tcp://0.0.0.0:$PORT",
    "install:frontend": "cd frontend && npm install"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
